"""ramen_cve.web — static-HTML Web UI builder (Task 8).

See `docs/web_ui_design.md` for the architecture. Subcommand entry
point: `ramen-cve web --site-dir <PATH>`. The L4 builder lives in
`web/builder.py`; the L4 CLI handler `_run_web` lives in `cli.py`
alongside the other subcommand runners.
"""
from __future__ import annotations
